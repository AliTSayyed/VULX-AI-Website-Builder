version: v2
managed:
  enabled: true
  disable: # prevent these imports to have the go pacakge prefix
    - module: buf.build/googleapis/googleapis
      file_option: go_package_prefix
    - module: buf.build/gnostic/gnostic
      file_option: go_package_prefix
  override: # ensure all code files generate in go have this prefix (allows us to import these files in go code)
    - file_option: go_package_prefix
      value: github.com/AliTSayyed/VULX-AI-Website-Builder/api/internal/infrastructure/inbound/grpc/gen
plugins: # tells buf where to find the compiler and where to out put results
  - local: app/node_modules/.bin/protoc-gen-es
    out: app/src/gen
    opt: target=ts
    include_imports: true # imports google annotations

  - remote: buf.build/protocolbuffers/go
    out: api/internal/infrastructure/inbound/grpc/gen
    opt: paths=source_relative

  - remote: buf.build/connectrpc/go
    out: api/internal/infrastructure/inbound/grpc/gen
    opt: paths=source_relative
