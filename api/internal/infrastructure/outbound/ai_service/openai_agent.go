package aiservice

/*
End points to trigger open ai llm from internal ai service.
Currently handles general queries and a coding agent (with some sandbox id)
*/

func (a *AIService) CallAI() error {
	return nil
}
